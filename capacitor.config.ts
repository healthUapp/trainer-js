import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.healthuapp.trainer',
  appName: 'Trainer',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
